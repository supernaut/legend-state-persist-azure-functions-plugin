pre-commit:
  parallel: true
  jobs:
    - run: pnpm exec prettier -uw {staged_files}
      stage_fixed: true

# Build commit messages
# prepare-commit-msg:
#   commands:
#     commitizen:
#       interactive: true
#       run: pnpm exec cz --hook # Or npx cz --hook
#       env:
#         LEFTHOOK: 0

# Validate commit messages
commit-msg:
  commands:
    'lint commit message':
      run: pnpm exec commitlint --edit {1}
